<Window x:Class="UniCast.App.ActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UniCast - Lisans Aktivasyonu" 
        Height="480" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EFEFEF"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#EFEFEF"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#9146FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#A366FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#555"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="#EFEFEF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4E4E52"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo ve Baslik -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,25">
            <TextBlock Text="🔐" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="UniCast Aktivasyon" 
                       FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Foreground="#9146FF"/>
        </StackPanel>

        <!-- Donanım ID -->
        <Border Grid.Row="1" Background="#2D2D30" CornerRadius="6" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Donanım Kimliği (Hardware ID)" 
                           FontSize="12" Foreground="#888" Margin="0,0,0,8"/>
                <Grid>
                    <TextBox x:Name="txtHardwareId" 
                             IsReadOnly="True" 
                             Text="Yükleniyor..."
                             FontSize="16"
                             Background="#1E1E1E"/>
                    <Button Content="📋" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"
                            Margin="0,0,5,0"
                            Width="35" Height="30"
                            Style="{StaticResource SecondaryButton}"
                            Click="BtnCopyHardwareId_Click"
                            ToolTip="Kopyala"/>
                </Grid>
                <TextBlock Text="Bu kimliği lisans satın alırken kullanın." 
                           FontSize="11" Foreground="#666" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Lisans Anahtarı Girişi -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Lisans Anahtarı" 
                       FontSize="13" Foreground="#AAA" Margin="0,0,0,8"/>
            <TextBox x:Name="txtLicenseKey" 
                     Height="45"
                     FontSize="18"
                     MaxLength="29"
                     CharacterCasing="Upper"
                     TextChanged="TxtLicenseKey_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background" Value="#2D2D30"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <TextBlock Text="Format: XXXXX-XXXXX-XXXXX-XXXXX-XXXXX" 
                       FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Durum Mesajı -->
        <Border Grid.Row="3" x:Name="StatusBorder" 
                Background="#332200" CornerRadius="4" 
                Padding="10" Margin="0,0,0,20"
                Visibility="Collapsed">
            <TextBlock x:Name="txtStatus" 
                       TextWrapping="Wrap" 
                       Foreground="#FFB74D"/>
        </Border>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="4" 
                     x:Name="progressBar" 
                     Height="4" 
                     IsIndeterminate="True"
                     Visibility="Collapsed"
                     Margin="0,0,0,20"/>

        <!-- Boşluk -->
        <Grid Grid.Row="5"/>

        <!-- Butonlar -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="🛒 Lisans Satın Al" 
                    Style="{StaticResource SecondaryButton}"
                    HorizontalAlignment="Left"
                    Click="BtnBuyLicense_Click"/>

            <Button Grid.Column="1" 
                    Content="İptal" 
                    Style="{StaticResource SecondaryButton}"
                    Margin="0,0,10,0"
                    Width="80"
                    Click="BtnCancel_Click"/>

            <Button Grid.Column="2" 
                    x:Name="btnActivate"
                    Content="✓ Aktifleştir" 
                    Style="{StaticResource PrimaryButton}"
                    Width="140"
                    IsEnabled="False"
                    Click="BtnActivate_Click"/>
        </Grid>
    </Grid>
</Window>