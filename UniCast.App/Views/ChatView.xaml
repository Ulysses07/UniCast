<UserControl x:Class="UniCast.App.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filters -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
            <CheckBox Content="YouTube"   IsChecked="{Binding ShowYouTube}"   Margin="0,0,8,0"/>
            <CheckBox Content="TikTok"    IsChecked="{Binding ShowTikTok}"    Margin="0,0,8,0"/>
            <CheckBox Content="Instagram" IsChecked="{Binding ShowInstagram}" Margin="0,0,8,0"/>
            <CheckBox Content="Facebook"  IsChecked="{Binding ShowFacebook}" />
        </StackPanel>

        <!-- Messages -->
        <ListBox Grid.Row="1" ItemsSource="{Binding View}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="0,0,0,6" CornerRadius="6" Background="#FF111111" Opacity="0.95">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <TextBlock Text="{Binding Source}" FontWeight="Bold" Foreground="#FFDDAA"/>
                                <TextBlock Text=" - " />
                                <TextBlock Text="{Binding Author}" FontWeight="SemiBold"/>
                                <TextBlock Text=" - " />
                                <TextBlock Text="{Binding Timestamp}" FontStyle="Italic" FontSize="11"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
