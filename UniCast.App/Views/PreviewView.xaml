<UserControl x:Class="UniCast.App.Views.PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#111">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#222" Padding="10" BorderBrush="#333" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="SAHNE EDİTÖRÜ: " Foreground="Gray" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>

                <Button Content="📷 Resim Ekle" Click="AddImage_Click" Margin="0,0,10,0" Background="#333" Foreground="White" Padding="10,5"/>
                <Button Content="🎥 Video Ekle" Click="AddVideo_Click" Margin="0,0,10,0" Background="#333" Foreground="White" Padding="10,5"/>
                <Button Content="🗑️ Seçileni Sil" Click="RemoveItem_Click" Background="#500" Foreground="White" Padding="10,5"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Image Source="{Binding PreviewImage}" Stretch="Uniform"/>

            <ItemsControl Name="EditorItemsControl" ItemsSource="{Binding SceneItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="{Binding Width}" Height="{Binding Height}"
                                BorderBrush="Orange" BorderThickness="2"
                                Background="#22000000"
                                Cursor="SizeAll"
                                MouseLeftButtonDown="Item_MouseDown"
                                MouseLeftButtonUp="Item_MouseUp"
                                MouseMove="Item_MouseMove"
                                Tag="{Binding}">

                            <Grid>
                                <TextBlock Text="{Binding Type}" Foreground="Orange" 
                                           VerticalAlignment="Center" HorizontalAlignment="Center" 
                                           FontWeight="Bold" IsHitTestVisible="False"/>

                                <Rectangle Name="ResizeHandle"
                                           Width="15" Height="15"
                                           Fill="Orange"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Bottom"
                                           Cursor="SizeNWSE"
                                           MouseLeftButtonDown="Resize_MouseDown"
                                           MouseLeftButtonUp="Resize_MouseUp"
                                           MouseMove="Resize_MouseMove"
                                           Tag="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>