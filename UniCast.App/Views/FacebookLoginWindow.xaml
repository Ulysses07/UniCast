<Window x:Class="UniCast.App.Views.FacebookLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Facebook'a Giriş Yap" 
        Height="700" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="500" MinWidth="400"
        Background="#1877F2">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#1877F2" Padding="15">
            <StackPanel>
                <TextBlock Text="🔵 Facebook Hesabına Giriş Yap" 
                           FontSize="18" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Chat mesajlarını okumak için Facebook hesabınıza giriş yapın." 
                           FontSize="12" Foreground="#E4E6EB" Margin="0,5,0,0"
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- WebView2 -->
        <Grid Grid.Row="1">
            <!-- Loading overlay -->
            <Border x:Name="LoadingOverlay" Background="#F0F2F5" Visibility="Visible" Panel.ZIndex="10">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtStatus" Text="Yükleniyor..." FontSize="14" 
                               Foreground="#65676B" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            
            <!-- WebView -->
            <wv2:WebView2 x:Name="webView" Visibility="Collapsed"/>
        </Grid>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F2F5" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔒" FontSize="14"/>
                    <TextBlock Text=" Şifreniz sadece Facebook'a gönderilir, UniCast'te saklanmaz." 
                               FontSize="11" Foreground="#65676B" VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button x:Name="BtnCancel" Grid.Column="1" Content="İptal" 
                        Width="80" Height="30" Click="BtnCancel_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
