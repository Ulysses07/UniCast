<UserControl x:Class="UniCast.App.Views.ChatOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:UniCast.Core.Models;assembly=UniCast.Core"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="{x:Null}">

    <UserControl.Resources>
        <DataTemplate x:Key="ChatTemplate">
            <Border Background="#66000000" CornerRadius="8" Padding="5">
                <ItemsControl ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ChatMessages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#CC000000" CornerRadius="4" Margin="0,2" Padding="5">
                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="White">
                                    <Run Text="{Binding Author}" FontWeight="Bold" Foreground="Gold"/>: 
                                    <Run Text="{Binding Text}"/>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="ImageTemplate">
            <Image Source="{Binding Source}" Stretch="Uniform"/>
        </DataTemplate>

        <DataTemplate x:Key="VideoTemplate">
            <MediaElement Source="{Binding Source}" 
                          LoadedBehavior="Play" 
                          UnloadedBehavior="Stop" 
                          MediaEnded="MediaElement_MediaEnded" 
                          Stretch="Uniform"/>
        </DataTemplate>
    </UserControl.Resources>

    <ItemsControl Name="SceneItemsControl" ItemsSource="{Binding SceneItems, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Left" Value="{Binding X}" />
                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                <Setter Property="Width" Value="{Binding Width}" />
                <Setter Property="Height" Value="{Binding Height}" />
                <Setter Property="Opacity" Value="{Binding Opacity}" />
                <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Style>
        </ItemsControl.ItemContainerStyle>

        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <ContentControl Content="{Binding}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Type}" Value="Chat">
                                    <Setter Property="ContentTemplate" Value="{StaticResource ChatTemplate}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Type}" Value="Image">
                                    <Setter Property="ContentTemplate" Value="{StaticResource ImageTemplate}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Type}" Value="Video">
                                    <Setter Property="ContentTemplate" Value="{StaticResource VideoTemplate}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>