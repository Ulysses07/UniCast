<Window x:Class="UniCast.App.Views.FirstRunWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UniCast - İlk Kurulum" 
        Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#1a1a2e">

    <Window.Resources>
        <Style x:Key="WizardButton" TargetType="Button">
            <Setter Property="Background" Value="#4361ee"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3a56d4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource WizardButton}">
            <Setter Property="Background" Value="#2d2d44"/>
        </Style>

        <Style x:Key="StepIndicator" TargetType="Ellipse">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Fill" Value="#3d3d5c"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16162a" Padding="20">
            <StackPanel>
                <TextBlock Text="🎬 UniCast Kurulum Sihirbazı" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="White"/>
                <TextBlock Text="Birkaç adımda yayın ayarlarınızı yapılandırın" 
                           FontSize="14" Foreground="#888" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <TabControl x:Name="WizardTabs" Grid.Row="1" 
                    Background="Transparent" BorderThickness="0"
                    Margin="20">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <!-- Step 1: Hoş Geldiniz -->
            <TabItem>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="👋" FontSize="64" HorizontalAlignment="Center"/>
                    <TextBlock Text="UniCast'e Hoş Geldiniz!" 
                               FontSize="28" FontWeight="Bold" 
                               Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    <TextBlock Text="Profesyonel canlı yayın uygulamanız hazır." 
                               FontSize="16" Foreground="#aaa" 
                               HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock TextWrapping="Wrap" Width="400" TextAlignment="Center"
                               Foreground="#888" Margin="0,30,0,0">
                        Bu sihirbaz size temel ayarları yapılandırmanızda yardımcı olacak.
                        YouTube, Twitch, TikTok, Instagram ve Facebook'ta aynı anda yayın yapabilirsiniz.
                    </TextBlock>
                </StackPanel>
            </TabItem>

            <!-- Step 2: Cihaz Seçimi -->
            <TabItem>
                <StackPanel Margin="40,20">
                    <TextBlock Text="📹 Cihaz Ayarları" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Yayında kullanacağınız kamera ve mikrofonu seçin" 
                               Foreground="#888" Margin="0,5,0,30"/>

                    <TextBlock Text="Kamera:" Foreground="White" FontSize="14" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CameraCombo" Height="35" Margin="0,0,0,20"/>

                    <TextBlock Text="Mikrofon:" Foreground="White" FontSize="14" Margin="0,0,0,5"/>
                    <ComboBox x:Name="MicrophoneCombo" Height="35" Margin="0,0,0,20"/>

                    <Button Content="🔄 Cihazları Yenile" Style="{StaticResource SecondaryButton}"
                            HorizontalAlignment="Left" Click="RefreshDevices_Click"/>
                </StackPanel>
            </TabItem>

            <!-- Step 3: Yayın Kalitesi -->
            <TabItem>
                <StackPanel Margin="40,20">
                    <TextBlock Text="📊 Yayın Kalitesi" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="İnternet hızınıza uygun kalite ayarı seçin" 
                               Foreground="#888" Margin="0,5,0,30"/>

                    <RadioButton x:Name="QualityLow" GroupName="Quality" Margin="0,10" Foreground="White">
                        <StackPanel>
                            <TextBlock Text="📶 Düşük Kalite (720p @ 2500 kbps)" FontWeight="Bold"/>
                            <TextBlock Text="Yavaş internet bağlantıları için önerilir" Foreground="#888" FontSize="12"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton x:Name="QualityMedium" GroupName="Quality" Margin="0,10" Foreground="White" IsChecked="True">
                        <StackPanel>
                            <TextBlock Text="📶📶 Orta Kalite (720p @ 4500 kbps)" FontWeight="Bold"/>
                            <TextBlock Text="Çoğu kullanıcı için önerilir" Foreground="#888" FontSize="12"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton x:Name="QualityHigh" GroupName="Quality" Margin="0,10" Foreground="White">
                        <StackPanel>
                            <TextBlock Text="📶📶📶 Yüksek Kalite (1080p @ 6000 kbps)" FontWeight="Bold"/>
                            <TextBlock Text="Hızlı internet bağlantısı gerektirir (10+ Mbps upload)" Foreground="#888" FontSize="12"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </TabItem>

            <!-- Step 4: Platform Seçimi -->
            <TabItem>
                <StackPanel Margin="40,20">
                    <TextBlock Text="🌐 Platform Seçimi" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Yayın yapacağınız platformları seçin (daha sonra değiştirebilirsiniz)" 
                               Foreground="#888" Margin="0,5,0,30"/>

                    <WrapPanel>
                        <CheckBox x:Name="ChkYouTube" Content="📺 YouTube" Foreground="White" Margin="0,10,30,10"/>
                        <CheckBox x:Name="ChkTwitch" Content="🟣 Twitch" Foreground="White" Margin="0,10,30,10"/>
                        <CheckBox x:Name="ChkTikTok" Content="🎵 TikTok" Foreground="White" Margin="0,10,30,10"/>
                        <CheckBox x:Name="ChkInstagram" Content="📷 Instagram" Foreground="White" Margin="0,10,30,10"/>
                        <CheckBox x:Name="ChkFacebook" Content="📘 Facebook" Foreground="White" Margin="0,10,30,10"/>
                    </WrapPanel>

                    <Border Background="#2d2d44" CornerRadius="5" Padding="15" Margin="0,30,0,0">
                        <StackPanel>
                            <TextBlock Text="💡 İpucu" FontWeight="Bold" Foreground="#4361ee"/>
                            <TextBlock TextWrapping="Wrap" Foreground="#aaa" Margin="0,5,0,0">
                                Platform stream key'lerini daha sonra Platformlar sekmesinden ekleyebilirsiniz.
                                Her platform için ilgili yayıncı panelinden stream key alınmalıdır.
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>

            <!-- Step 5: Tamamlandı -->
            <TabItem>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="✅" FontSize="64" HorizontalAlignment="Center"/>
                    <TextBlock Text="Kurulum Tamamlandı!" 
                               FontSize="28" FontWeight="Bold" 
                               Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    <TextBlock Text="UniCast kullanıma hazır!" 
                               FontSize="16" Foreground="#4361ee" 
                               HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock TextWrapping="Wrap" Width="400" TextAlignment="Center"
                               Foreground="#888" Margin="0,30,0,0">
                        Ayarlarınız kaydedildi. Artık yayın yapmaya başlayabilirsiniz.
                        Daha fazla ayar için Ayarlar sekmesini ziyaret edebilirsiniz.
                    </TextBlock>

                    <CheckBox x:Name="ChkDontShowAgain" 
                              Content="Bu sihirbazı bir daha gösterme" 
                              Foreground="#888" Margin="0,40,0,0" 
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#16162a" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button -->
                <Button x:Name="BtnBack" Content="← Geri" 
                        Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Left"
                        Click="Back_Click" Visibility="Collapsed"/>

                <!-- Step Indicators -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse x:Name="Step1Indicator" Style="{StaticResource StepIndicator}" Fill="#4361ee"/>
                    <Ellipse x:Name="Step2Indicator" Style="{StaticResource StepIndicator}"/>
                    <Ellipse x:Name="Step3Indicator" Style="{StaticResource StepIndicator}"/>
                    <Ellipse x:Name="Step4Indicator" Style="{StaticResource StepIndicator}"/>
                    <Ellipse x:Name="Step5Indicator" Style="{StaticResource StepIndicator}"/>
                </StackPanel>

                <!-- Next/Finish Button -->
                <Button x:Name="BtnNext" Content="İleri →" 
                        Style="{StaticResource WizardButton}"
                        Grid.Column="2" HorizontalAlignment="Right"
                        Click="Next_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
