<UserControl x:Class="UniCast.App.Views.ControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="Start" Command="{Binding StartCommand}" Width="120" Margin="0,0,8,0"/>
            <Button Content="Stop"  Command="{Binding StopCommand}"  Width="120"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Durum: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Status}" Margin="8,0,16,0"/>
            <TextBlock Text="Metrik: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Metric}"/>
        </StackPanel>
        <TextBlock Text="{Binding Advisory}"
           Foreground="DarkOrange"
           TextWrapping="Wrap"
           Margin="0,4,0,0"/>

        <Border Grid.Row="2" BorderBrush="#333" BorderThickness="1" CornerRadius="6" Background="#000">
            <Image Stretch="Uniform" Source="{Binding PreviewImage}"/>
        </Border>
    </Grid>
</UserControl>
