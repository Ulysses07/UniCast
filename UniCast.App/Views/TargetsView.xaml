<UserControl x:Class="UniCast.App.Views.TargetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#1b1b1f">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="RTMP/RTMPS Hedefleri" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

        <Border Grid.Row="1" Background="#222" CornerRadius="8" Padding="8">
            <ItemsControl ItemsSource="{Binding Targets}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="90"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" Foreground="#e5e7eb" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                                     Background="#2b2b33" Foreground="#f8fafc" BorderBrush="#3b3b44" />
                            <CheckBox Grid.Column="2" Content="Aktif"
                                      IsChecked="{Binding Enabled}"
                                      Foreground="#e5e7eb" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="Uyarılar:" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Warnings}" Foreground="#fca5a5" Margin="8,0,0,0"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
