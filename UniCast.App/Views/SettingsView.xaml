<UserControl x:Class="UniCast.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="1200">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel MaxWidth="800" Width="761">

            <TextBlock Text="⚙️ Ayarlar" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource TextMain}"/>

            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Kamera ve Mikrofon" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Kamera" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>

                        <ComboBox Grid.Column="1" ItemsSource="{Binding VideoDevices}"
                                  SelectedValue="{Binding DefaultCamera, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Name" 
                                  Height="35">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Foreground="Black"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <Button Grid.Column="2" Content="🔄" Command="{Binding RefreshDevicesCommand}" 
                                Width="40" Height="35" Margin="10,0,0,0" ToolTip="Cihazları Yenile"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Mikrofon" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>

                        <ComboBox Grid.Column="1" ItemsSource="{Binding AudioDevices}"
                                  SelectedValue="{Binding DefaultMicrophone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Name"
                                  Height="35">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Foreground="Black"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Yayın Kalitesi" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Video Encoder" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding Encoder, Mode=TwoWay}" 
                                  SelectedValuePath="Tag" Height="35">
                            <ComboBoxItem Tag="auto">
                                <TextBlock Text="Otomatik (Önerilen)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="h264_nvenc">
                                <TextBlock Text="NVIDIA NVENC (GPU)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="h264_amf">
                                <TextBlock Text="AMD AMF (GPU)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="h264_qsv">
                                <TextBlock Text="Intel QuickSync (GPU)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="libx264">
                                <TextBlock Text="CPU (libx264)" Foreground="Black"/>
                            </ComboBoxItem>
                        </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Çözünürlük" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Width}" Width="80" Height="30" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                            <TextBlock Text="x" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextMuted}"/>
                            <TextBox Text="{Binding Height}" Width="80" Height="30" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="FPS" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <TextBox Grid.Column="1" Text="{Binding Fps}" Height="30" VerticalContentAlignment="Center"/>

                        <TextBlock Grid.Column="3" Text="Video Bitrate (Kbps)" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <TextBox Grid.Column="4" Text="{Binding VideoKbps}" Height="30" VerticalContentAlignment="Center"/>
                    </Grid>

                    <TextBlock Text="Ses Senkronizasyonu (Gecikme)" Margin="0,10,0,5" Foreground="{StaticResource TextMuted}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <Slider Minimum="0" Maximum="2000" TickFrequency="50" IsSnapToTickEnabled="True"
                                Value="{Binding AudioDelayMs}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"
                                   Text="{Binding AudioDelayMs, StringFormat={}{0} ms}" Foreground="{StaticResource TextMuted}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- YouTube Ayarları -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="YouTube Chat Ayarları" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <TextBlock Text="Video ID veya URL" Foreground="{StaticResource TextMuted}" Margin="0,5"/>
                    <TextBox Text="{Binding YouTubeVideoId, UpdateSourceTrigger=PropertyChanged}" Height="30"
                             ToolTip="Örnek: https://youtube.com/watch?v=XXXXX veya sadece XXXXX"/>
                    <TextBlock Text="Örnek: https://youtube.com/watch?v=abc123 veya sadece abc123" 
                               FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Twitch Chat Ayarları -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Twitch Chat Ayarları" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <TextBlock Text="Kanal Adı" Foreground="{StaticResource TextMuted}" Margin="0,5"/>
                    <TextBox Text="{Binding TwitchChannelName, UpdateSourceTrigger=PropertyChanged}" Height="30"
                             ToolTip="Twitch kanal adınız (örn: ninja)"/>

                    <TextBlock Text="Bot Kullanıcı Adı (Opsiyonel)" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding TwitchBotUsername, UpdateSourceTrigger=PropertyChanged}" Height="30"
                             ToolTip="Chat botu kullanıcı adı (boş bırakılabilir)"/>

                    <TextBlock Text="OAuth Token" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                    <PasswordBox x:Name="PwdTwitchOAuth" PasswordChanged="PwdTwitchOAuth_PasswordChanged" Height="30"
                                ToolTip="https://twitchapps.com/tmi/ adresinden alabilirsiniz"/>
                    <TextBlock Text="Token almak için: https://twitchapps.com/tmi/" 
                               FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Sosyal Medya Entegrasyonu" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <!-- Instagram Ayarları -->
                    <Expander Header="Instagram Ayarları" Margin="0,0,0,10" Foreground="{StaticResource TextMain}">
                        <StackPanel Margin="10">
                            <!-- KRİTİK UYARI -->
                            <Border Background="#FFF3CD" BorderBrush="#FFE69C" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="⚠️ ÖNEMLİ GÜVENLİK UYARISI" FontWeight="Bold" Foreground="#856404" FontSize="12"/>
                                    <TextBlock TextWrapping="Wrap" Foreground="#856404" FontSize="11" Margin="0,5,0,0">
                                        Instagram Private API kullanımı hesap yasaklanmasına yol açabilir!
                                        Ana hesabınızı DEĞİL, sadece chat okumak için oluşturduğunuz
                                        AYRI BİR OKUYUCU HESAP kullanın. UniCast bu hesabın
                                        yasaklanmasından sorumlu değildir.
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <TextBlock Text="Okuyucu Hesap Kullanıcı Adı" Foreground="{StaticResource TextMuted}" Margin="0,5"/>
                            <TextBox Text="{Binding InstagramUserId, UpdateSourceTrigger=PropertyChanged}" Height="30"
                                     ToolTip="Ana hesabınız DEĞİL, ayrı bir okuyucu hesap kullanın!"/>

                            <TextBlock Text="Okuyucu Hesap Şifresi" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                            <PasswordBox x:Name="PwdInstagramSession" PasswordChanged="PwdInstagramSession_PasswordChanged" Height="30"
                                        ToolTip="Private API için gerekli. Ana hesap şifrenizi GİRMEYİN!"/>

                            <TextBlock Text="Yayıncı Hesap Kullanıcı Adı" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                            <TextBox Text="{Binding InstagramBroadcasterUsername, UpdateSourceTrigger=PropertyChanged}" Height="30"
                                     ToolTip="Canlı yayın açtığınız hesabın kullanıcı adı (örn: emaroutlet)"/>
                            <TextBlock Text="Yayın açtığınız hesabın kullanıcı adını girin. Boş bırakırsanız okuyucu hesap aranır." 
                                       Foreground="{StaticResource TextMuted}" FontSize="10" Margin="0,3,0,0"/>

                            <TextBlock Text="Not: Instagram canlı chat için hesapta aktif canlı yayın gerekir." 
                                       Foreground="Orange" FontSize="10" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Expander>

                    <!-- Facebook Ayarları (YENİ: Okuyucu Hesap Yaklaşımı) -->
                    <Expander Header="Facebook Ayarları" Foreground="{StaticResource TextMain}">
                        <StackPanel Margin="10">

                            <!-- YÖNTEM SEÇİMİ -->
                            <TextBlock Text="Bağlantı Yöntemi" FontWeight="SemiBold" Foreground="{StaticResource TextMain}" Margin="0,0,0,10"/>

                            <RadioButton x:Name="RbFacebookScraping" 
                                         Content="🔍 WebView2 Scraping (Tüm yayınlar için)" 
                                         GroupName="FacebookMethod"
                                         IsChecked="{Binding FacebookUseGraphApi, Converter={StaticResource InverseBoolConverter}, Mode=TwoWay}"
                                         Foreground="{StaticResource TextMain}"
                                         Margin="0,0,0,5"/>

                            <RadioButton x:Name="RbFacebookApi" 
                                         Content="🔗 Graph API (Kendi sayfanız için - Önerilen)" 
                                         GroupName="FacebookMethod"
                                         IsChecked="{Binding FacebookUseGraphApi, Mode=TwoWay}"
                                         Foreground="{StaticResource TextMain}"
                                         Margin="0,0,0,15"/>

                            <!-- ========== GRAPH API BÖLÜMÜ ========== -->
                            <Border x:Name="FacebookApiSection" 
                                    Background="#E8F5E9" BorderBrush="#A5D6A7" BorderThickness="1" 
                                    CornerRadius="4" Padding="15" Margin="0,0,0,15"
                                    Visibility="{Binding FacebookUseGraphApi, Converter={StaticResource BoolToVisibility}}">
                                <StackPanel>
                                    <TextBlock Text="📡 Facebook Graph API" FontWeight="Bold" Foreground="#2E7D32" FontSize="14"/>
                                    <TextBlock Text="Kendi Facebook sayfanızın canlı yayın yorumlarını resmi API ile çekin." 
                                               Foreground="#388E3C" FontSize="11" Margin="0,5,0,10" TextWrapping="Wrap"/>

                                    <!-- API Bağlantı Durumu -->
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Ellipse x:Name="FacebookApiStatusIndicator" Width="12" Height="12" 
                                                     Fill="Gray" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="TxtFacebookApiStatus" Text="Sayfa bağlı değil" 
                                                       FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                                        </StackPanel>

                                        <Button x:Name="BtnFacebookApiConnect" Grid.Column="1" 
                                                Content="🔗 Sayfa Bağla" 
                                                Click="BtnFacebookApiConnect_Click"
                                                Background="#4CAF50" Foreground="White"
                                                Padding="15,8" FontWeight="SemiBold"
                                                BorderThickness="0" Cursor="Hand"/>
                                    </Grid>

                                    <!-- Bağlı Sayfa Bilgisi -->
                                    <StackPanel x:Name="FacebookApiPageInfo" Visibility="Collapsed" Margin="0,5,0,0">
                                        <TextBlock Text="Bağlı Sayfa:" Foreground="#2E7D32" FontSize="11"/>
                                        <TextBlock x:Name="TxtFacebookPageName" Text="-" FontWeight="SemiBold" 
                                                   Foreground="#1B5E20" FontSize="13" Margin="0,2,0,0"/>
                                        <TextBlock x:Name="TxtFacebookTokenExpiry" Text="" 
                                                   Foreground="#689F38" FontSize="10" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Gereksinimler -->
                                    <Border Background="#FFF8E1" BorderBrush="#FFE082" BorderThickness="1" 
                                            CornerRadius="4" Padding="10" Margin="0,10,0,0">
                                        <StackPanel>
                                            <TextBlock Text="📋 Gereksinimler:" FontWeight="SemiBold" Foreground="#F57C00" FontSize="11"/>
                                            <TextBlock TextWrapping="Wrap" Foreground="#EF6C00" FontSize="10" Margin="0,5,0,0">
                                                • Facebook Sayfası (kişisel profil değil)
                                                • Sayfa en az 60 günlük olmalı
                                                • Sayfa en az 100 takipçiye sahip olmalı
                                                • Sayfada Admin/Editor rolünüz olmalı
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- ========== SCRAPING BÖLÜMÜ (ESKİ) ========== -->
                            <Border x:Name="FacebookScrapingSection"
                                    Visibility="{Binding FacebookUseGraphApi, Converter={StaticResource InverseBoolToVisibility}}">
                                <StackPanel>
                                    <!-- KRİTİK UYARI (Instagram ile aynı stil) -->
                                    <Border Background="#FFF3CD" BorderBrush="#FFE69C" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="⚠️ ÖNEMLİ GÜVENLİK UYARISI" FontWeight="Bold" Foreground="#856404" FontSize="12"/>
                                            <TextBlock TextWrapping="Wrap" Foreground="#856404" FontSize="11" Margin="0,5,0,0">
                                                Facebook chat için ANA HESABINIZI DEĞİL, sadece yayını izlemek 
                                                için oluşturduğunuz AYRI BİR OKUYUCU HESAP kullanın!
                                                Ana hesabınızda güvenlik kontrolü (checkpoint) tetiklenebilir.
                                                UniCast okuyucu hesabın yasaklanmasından sorumlu değildir.
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>

                                    <!-- Bağlantı Durumu -->
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Ellipse x:Name="FacebookStatusIndicator" Width="12" Height="12" 
                                                     Fill="Gray" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="TxtFacebookStatus" Text="Bağlı Değil" 
                                                       FontSize="14" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                                        </StackPanel>

                                        <Button x:Name="BtnFacebookConnect" Grid.Column="1" 
                                                Content="🔵 Bağlan" 
                                                Click="BtnFacebookConnect_Click"
                                                Background="#1877F2" Foreground="White"
                                                Padding="15,8" FontWeight="SemiBold"
                                                BorderThickness="0" Cursor="Hand"/>
                                    </Grid>

                                    <!-- Okuyucu Hesap Bilgileri -->
                                    <TextBlock Text="Okuyucu Hesap E-posta/Telefon" Foreground="{StaticResource TextMuted}" Margin="0,5"/>
                                    <TextBox x:Name="TxtFacebookReaderEmail" Height="30"
                                             Text="{Binding FacebookReaderEmail, UpdateSourceTrigger=PropertyChanged}"
                                             ToolTip="Ana hesabınız DEĞİL, ayrı bir okuyucu hesap kullanın!"/>

                                    <TextBlock Text="Okuyucu Hesap Şifresi" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                                    <PasswordBox x:Name="PwdFacebookReaderPassword" 
                                                 PasswordChanged="PwdFacebookReaderPassword_PasswordChanged" Height="30"
                                                 ToolTip="Okuyucu hesap şifresi. Ana hesap şifrenizi GİRMEYİN!"/>

                                    <!-- Canlı Yayın Linki -->
                                    <TextBlock Text="Facebook Canlı Yayın Linki" Foreground="{StaticResource TextMuted}" Margin="0,15,0,5"/>
                                    <TextBox Text="{Binding FacebookLiveVideoUrl, UpdateSourceTrigger=PropertyChanged}" Height="30"
                                             ToolTip="Ana hesabınızla yaptığınız canlı yayının linki"/>
                                    <TextBlock Text="Örnek: https://facebook.com/sayfa/videos/123456789 veya sadece video ID" 
                                               FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>

                                    <!-- Çıkış Butonu -->
                                    <Button x:Name="BtnFacebookLogout" Content="🚪 Çıkış Yap" 
                                            Click="BtnFacebookLogout_Click"
                                            HorizontalAlignment="Left" Margin="0,15,0,0"
                                            Padding="10,5" Visibility="Collapsed"
                                            Background="#DC3545" Foreground="White" BorderThickness="0"/>

                                    <!-- Yardım Notu -->
                                    <Border Background="#E7F3FF" BorderBrush="#B6D4FE" BorderThickness="1" 
                                            CornerRadius="4" Padding="10" Margin="0,15,0,0">
                                        <StackPanel>
                                            <TextBlock Text="💡 Nasıl Çalışır?" FontWeight="SemiBold" Foreground="#084298" FontSize="11"/>
                                            <TextBlock TextWrapping="Wrap" Foreground="#084298" FontSize="10" Margin="0,5,0,0">
                                                1. Yeni bir Facebook hesabı oluşturun (okuyucu hesap)
                                                2. Yukarıya bu hesabın bilgilerini girin
                                                3. ANA HESABINIZLA canlı yayın başlatın
                                                4. Yayın linkini "Canlı Yayın Linki" alanına yapıştırın
                                                5. UniCast okuyucu hesapla chatı çekecektir
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Yerel Kayıt" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <CheckBox Content="Yayın sırasında bilgisayara da kaydet (MP4)" 
                              IsChecked="{Binding EnableLocalRecord}" Margin="0,0,0,10" Foreground="{StaticResource TextMain}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding RecordFolder}" IsReadOnly="True" Height="35" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="📂 Seç" Command="{Binding BrowseRecordFolderCommand}" 
                                Margin="10,0,0,0" Height="35" Width="60"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Button Content="💾 Ayarları Kaydet" Command="{Binding SaveCommand}" 
                    HorizontalAlignment="Right" Width="200" Height="50" 
                    Background="{StaticResource Primary}" BorderThickness="0" FontWeight="Bold" FontSize="16"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
