<UserControl x:Class="UniCast.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="1200">

    <UserControl.Resources>
        <!-- Kaydet Butonu Stili -->
        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#7C3AED"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleX" 
                                                             To="1.02" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleY" 
                                                             To="1.02" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleX" 
                                                             To="1" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleY" 
                                                             To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5B21B6"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleX" 
                                                             To="0.95" Duration="0:0:0.05"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleY" 
                                                             To="0.95" Duration="0:0:0.05"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleX" 
                                                             To="1.02" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                             Storyboard.TargetProperty="ScaleY" 
                                                             To="1.02" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                                <Setter Property="Foreground" Value="#D1D5DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel MaxWidth="800" Width="761">

            <TextBlock Text="⚙️ Ayarlar" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource TextMain}"/>

            <!-- ========== KAMERA VE MİKROFON ========== -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Kamera ve Mikrofon" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Kamera" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>

                        <ComboBox Grid.Column="1" ItemsSource="{Binding VideoDevices}"
                                  SelectedValue="{Binding DefaultCamera, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Name" 
                                  Height="35">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Foreground="Black"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <Button Grid.Column="2" Content="🔄" Command="{Binding RefreshDevicesCommand}" 
                                Width="40" Height="35" Margin="10,0,0,0" ToolTip="Cihazları Yenile"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Mikrofon" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>

                        <ComboBox Grid.Column="1" ItemsSource="{Binding AudioDevices}"
                                  SelectedValue="{Binding DefaultMicrophone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedValuePath="Name"
                                  Height="35">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Foreground="Black"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ========== YAYIN KALİTESİ ========== -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Yayın Kalitesi" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Video Encoder" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding Encoder, Mode=TwoWay}" 
                                  SelectedValuePath="Tag" Height="35">
                            <ComboBoxItem Tag="auto">
                                <TextBlock Text="Otomatik (Önerilen)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="h264_nvenc">
                                <TextBlock Text="NVIDIA NVENC (GPU)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="h264_amf">
                                <TextBlock Text="AMD AMF (GPU)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="h264_qsv">
                                <TextBlock Text="Intel QuickSync (GPU)" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="libx264">
                                <TextBlock Text="CPU (libx264)" Foreground="Black"/>
                            </ComboBoxItem>
                        </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Çözünürlük" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Width}" Width="80" Height="30" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                            <TextBlock Text="x" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextMuted}"/>
                            <TextBox Text="{Binding Height}" Width="80" Height="30" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="FPS" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <TextBox Grid.Column="1" Text="{Binding Fps}" Height="30" VerticalContentAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Video Bitrate" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <TextBox Grid.Column="4" Text="{Binding VideoKbps}" Height="30" VerticalContentAlignment="Center"/>
                    </Grid>

                    <!-- Kamera Döndürme (9:16 dikey yayın için) -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Kamera Döndürme" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding CameraRotation, Mode=TwoWay}" 
                                  SelectedValuePath="Tag" Height="35" MaxWidth="300" HorizontalAlignment="Left">
                            <ComboBoxItem Tag="0">
                                <TextBlock Text="0° - Döndürme Yok" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="90">
                                <TextBlock Text="90° - Saat Yönünde" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="180">
                                <TextBlock Text="180° - Baş Aşağı" Foreground="Black"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="270">
                                <TextBlock Text="270° - Saat Yönü Tersi" Foreground="Black"/>
                            </ComboBoxItem>
                        </ComboBox>
                    </Grid>
                    <TextBlock Text="💡 Dikey (9:16) yayın için: Kamerayı fiziksel olarak 90° döndürün, sonra 270° seçin" 
                               FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,0,0,10"/>

                    <TextBlock Text="Ses Senkronizasyonu (Gecikme)" Margin="0,10,0,5" Foreground="{StaticResource TextMuted}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <Slider Minimum="0" Maximum="2000" TickFrequency="50" IsSnapToTickEnabled="True"
                                Value="{Binding AudioDelayMs}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"
                                   Text="{Binding AudioDelayMs, StringFormat={}{0} ms}" Foreground="{StaticResource TextMuted}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ========== SOSYAL MEDYA ENTEGRASYONU ========== -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Sosyal Medya Entegrasyonu" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <!-- YouTube Ayarları -->
                    <Expander Header="▶ YouTube Ayarları" Margin="0,0,0,10" Foreground="{StaticResource TextMain}">
                        <StackPanel Margin="10">
                            <TextBlock Text="Video ID veya URL" Foreground="{StaticResource TextMuted}" Margin="0,5"/>
                            <TextBox Text="{Binding YouTubeVideoId, UpdateSourceTrigger=PropertyChanged}" Height="30"
                                     ToolTip="Örnek: https://youtube.com/watch?v=XXXXX veya sadece XXXXX"/>
                            <TextBlock Text="Örnek: https://youtube.com/watch?v=abc123 veya sadece abc123" 
                                       FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,5,0,0"/>

                            <Border Background="#FFEBEE" BorderBrush="#FFCDD2" BorderThickness="1" 
                                    CornerRadius="4" Padding="10" Margin="0,10,0,0">
                                <TextBlock TextWrapping="Wrap" Foreground="#C62828" FontSize="10">
                                    💡 YouTube chat için: Yayın başlattığınızda video ID'yi buraya girin.
                                    Chat mesajları otomatik olarak çekilecektir.
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Twitch Ayarları -->
                    <Expander Header="◆ Twitch Ayarları" Margin="0,0,0,10" Foreground="{StaticResource TextMain}">
                        <StackPanel Margin="10">
                            <TextBlock Text="Kanal Adı" Foreground="{StaticResource TextMuted}" Margin="0,5"/>
                            <TextBox Text="{Binding TwitchChannelName, UpdateSourceTrigger=PropertyChanged}" Height="30"
                                     ToolTip="Twitch kanal adınız (örn: ninja)"/>

                            <TextBlock Text="Bot Kullanıcı Adı (Opsiyonel)" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                            <TextBox Text="{Binding TwitchBotUsername, UpdateSourceTrigger=PropertyChanged}" Height="30"
                                     ToolTip="Chat botu kullanıcı adı (boş bırakılabilir)"/>

                            <TextBlock Text="OAuth Token" Foreground="{StaticResource TextMuted}" Margin="0,10,0,5"/>
                            <PasswordBox x:Name="PwdTwitchOAuth" PasswordChanged="PwdTwitchOAuth_PasswordChanged" Height="30"
                                        ToolTip="https://twitchapps.com/tmi/ adresinden alabilirsiniz"/>

                            <Border Background="#EDE7F6" BorderBrush="#D1C4E9" BorderThickness="1" 
                                    CornerRadius="4" Padding="10" Margin="0,10,0,0">
                                <TextBlock TextWrapping="Wrap" Foreground="#512DA8" FontSize="10">
                                    🔗 Token almak için: https://twitchapps.com/tmi/
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Browser Extension Bilgisi -->
                    <Border Background="#E8F5E9" BorderBrush="#A5D6A7" BorderThickness="1" 
                            CornerRadius="4" Padding="15" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="🌐 Instagram, Facebook, TikTok Chat" FontWeight="Bold" 
                                       Foreground="#2E7D32" FontSize="14"/>
                            <TextBlock TextWrapping="Wrap" Foreground="#388E3C" FontSize="11" Margin="0,10,0,0">
                                Bu platformlar için UniCast Chat Bridge tarayıcı extension'ı kullanılıyor.
                                Extension otomatik olarak açık olan Live sayfalarından yorumları çeker.
                            </TextBlock>
                            <TextBlock TextWrapping="Wrap" Foreground="#1B5E20" FontSize="11" Margin="0,10,0,0" FontWeight="SemiBold">
                                Kullanım:
                            </TextBlock>
                            <TextBlock TextWrapping="Wrap" Foreground="#388E3C" FontSize="11" Margin="0,5,0,0">
                                1. Chrome'da UniCast Chat Bridge extension'ını yükleyin
                                2. İlgili platforma giriş yapın (tarayıcıda)
                                3. Live sayfasını açın (Instagram/Facebook/TikTok)
                                4. Yorumlar otomatik olarak UniCast'e aktarılır
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ========== STREAM CHAT OVERLAY ========== -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="📺 Yayın Chat Overlay" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,15">
                        Chat mesajlarını doğrudan yayın görüntüsüne ekler. Tüm platformlardaki izleyiciler chatı görebilir.
                    </TextBlock>

                    <CheckBox x:Name="ChkStreamChatOverlay" 
                              Content="Chat overlay'i yayına ekle" 
                              IsChecked="{Binding StreamChatOverlayEnabled, Mode=TwoWay}" 
                              Margin="0,0,0,15" 
                              FontWeight="SemiBold"
                              Foreground="{StaticResource TextMain}"/>

                    <StackPanel Visibility="{Binding IsChecked, ElementName=ChkStreamChatOverlay, Converter={StaticResource BoolToVisibility}}">

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Pozisyon" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                            <ComboBox Grid.Column="1" 
                                      SelectedValue="{Binding StreamChatOverlayPosition, Mode=TwoWay}" 
                                      SelectedValuePath="Tag" 
                                      Height="35">
                                <ComboBoxItem Tag="BottomLeft">
                                    <TextBlock Text="Sol Alt" Foreground="Black"/>
                                </ComboBoxItem>
                                <ComboBoxItem Tag="BottomRight">
                                    <TextBlock Text="Sağ Alt" Foreground="Black"/>
                                </ComboBoxItem>
                                <ComboBoxItem Tag="TopLeft">
                                    <TextBlock Text="Sol Üst" Foreground="Black"/>
                                </ComboBoxItem>
                                <ComboBoxItem Tag="TopRight">
                                    <TextBlock Text="Sağ Üst" Foreground="Black"/>
                                </ComboBoxItem>
                                <ComboBoxItem Tag="Center">
                                    <TextBlock Text="Orta" Foreground="Black"/>
                                </ComboBoxItem>
                            </ComboBox>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Max Mesaj" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                            <Slider Grid.Column="1" 
                                    Minimum="1" Maximum="15" 
                                    Value="{Binding StreamChatOverlayMaxMessages, Mode=TwoWay}"
                                    TickFrequency="1" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding StreamChatOverlayMaxMessages}" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextMain}"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Mesaj Süresi" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                            <Slider Grid.Column="1" 
                                    Minimum="5" Maximum="120" 
                                    Value="{Binding StreamChatOverlayMessageLifetime, Mode=TwoWay}"
                                    TickFrequency="5" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextMain}">
                                <Run Text="{Binding StreamChatOverlayMessageLifetime, Mode=OneWay}"/>
                                <Run Text="sn"/>
                            </TextBlock>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Font Boyutu" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                            <Slider Grid.Column="1" 
                                    Minimum="12" Maximum="48" 
                                    Value="{Binding StreamChatOverlayFontSize, Mode=TwoWay}"
                                    TickFrequency="2" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextMain}">
                                <Run Text="{Binding StreamChatOverlayFontSize, Mode=OneWay}"/>
                                <Run Text="px"/>
                            </TextBlock>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Şeffaflık" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                            <Slider Grid.Column="1" 
                                    Minimum="0.1" Maximum="1.0" 
                                    Value="{Binding StreamChatOverlayOpacity, Mode=TwoWay}"
                                    TickFrequency="0.1" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding StreamChatOverlayOpacity, StringFormat=P0}" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextMain}"/>
                        </Grid>

                        <CheckBox Content="Gölge efekti" 
                                  IsChecked="{Binding StreamChatOverlayShadow, Mode=TwoWay}" 
                                  Foreground="{StaticResource TextMain}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ========== YEREL KAYIT ========== -->
            <Border Background="{StaticResource BgLight}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Yerel Kayıt" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource Primary}"/>

                    <CheckBox Content="Yayın sırasında bilgisayara da kaydet (MP4)" 
                              IsChecked="{Binding EnableLocalRecord}" Margin="0,0,0,10" Foreground="{StaticResource TextMain}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding RecordFolder}" IsReadOnly="True" Height="35" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="📂 Seç" Command="{Binding BrowseRecordFolderCommand}" 
                                Margin="10,0,0,0" Height="35" Width="60"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ========== KAYDET BUTONU ========== -->
            <Button Content="💾 Ayarları Kaydet" Command="{Binding SaveCommand}" 
                    HorizontalAlignment="Right" Width="200" Height="50" 
                    Style="{StaticResource SaveButtonStyle}"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
